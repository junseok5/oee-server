type SocialFields {
    id: String
    accessToken: String
}

type Social {
    facebook: SocialFields
    google: SocialFields
}

type User {
    _id: String!
    email: String!
    social: Social
    displayName: String!
    thumbnail: String!
    createdAt: String!
    updatedAt: String!
}
