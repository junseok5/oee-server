type User {
    _id: String!
    email: String!
    fbId: String
    fbToken: String
    googleId: String
    googleToken: String
    displayName: String!
    thumbnail: String!
    createdAt: String!
    updatedAt: String!
}
